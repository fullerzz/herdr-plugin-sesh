# Implementation Plans

Generated by the improve skill on 2026-08-22 against commit `d84f49b`.
Execute plans in the order below unless dependencies say otherwise. Each
executor must read the selected plan fully, honor its STOP conditions, run all
verification gates, and update its status row when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| [001](001-show-worktree-parentage.md) | Show linked-worktree parentage in the native picker | P1 | M | — | DONE |

Status values: `TODO` · `IN PROGRESS` · `DONE` · `BLOCKED` (with one-line
reason) · `REJECTED` (with one-line rationale).

## Dependency notes

- Plan 001 is self-contained and has no prerequisite plan.
- Review decision: resolved parents must appear immediately above their linked
  children, matching Herdr's sidebar. The plan groups families in both workspace
  and recent modes; a family's best member rank positions the group while the
  parent remains first.

## Findings considered and rejected

- Add the indicator to fzf simultaneously: deferred because the request targets
  the native picker and fzf has a separate tab-delimited rendering pipeline.
- Detect worktrees by running Git or `herdr worktree list` per workspace:
  rejected because `herdr workspace list` already supplies the required
  metadata and extra subprocesses would add latency and failure modes.
